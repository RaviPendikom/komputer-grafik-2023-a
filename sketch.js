let x = 300;
let y = 300;
let radius = 250;

function setup() {
  createCanvas(600, 600);
  background(132, 49, 120);
  noStroke();
  fill(152, 200, 89);
  circle(x, y, radius);
  square(x, y, radius);
}
